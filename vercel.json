{
  "version": 2,
  "name": "medflow",
  "installCommand": "rm -rf node_modules frontend/node_modules backend/node_modules && npm install --include=dev && node node_modules/prisma/build/index.js generate --schema=backend/prisma/schema.prisma && npm run --workspace=backend build && npm run --workspace=frontend build",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "backend/dist/**",
          "backend/node_modules/**",
          "backend/prisma/**",
          "node_modules/**"
        ]
      }
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static",
      "config": {
        "outputDirectory": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/$1"
    }
  ]
}
